# Run memcached as a daemon.
-d

# Run as the 'memcache' user.
-u memcache

# Log memcached's output to /var/log/memcached.
logfile /var/log/memcached.log

# Listen on port 11211 (the default).
-p 11211

# Start with a cap of 4GB of memory. Note that the daemon will grow to this
# size, but does not allocate it all to start with.
-m 4096
